import React from 'react';
import { Navbar } from '../../components';
import './header.css';

function Header(props) {
  return (
    <header>
      <Navbar />
    </header>
  );
}

export default Header;